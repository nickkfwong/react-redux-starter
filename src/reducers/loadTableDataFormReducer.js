import {REQUEST_DATA} from '../constants/actionTypes';

const loadTableDataFormReducer = (state, action) =>
{
    switch(action.type)
    {
        default:
            return state;
    }
    return state;
}

export default loadTableDataFormReducer;